# Separate repository — lives at github.com/Hakevesc/RemAssistance
RemAssistance/

# Playwright MCP scratch output
.playwright-mcp/

# OS / editor cruft
Thumbs.db
.DS_Store

# Next.js
node_modules/
.next/
*.tsbuildinfo
.env*
# ...but the documented template is meant to be committed
!.env.example

# Dev server log
dev.log

# Legacy DC static site — every .dc.html artboard, index.html, the web-loader
# and partials/, collected under one roof and kept out of the push. The Next.js
# app in app/ serves all of these routes now; the markup stays on disk (and in
# git history before this commit) as the port's reference.
legacy-html/
